{{#pagination posts=model as |pagedPosts| }}
  <ul class="results">
    {{#each pagedPosts as |singlePost|}}
      <IndividualPost @post = {{singlePost}} />
    {{/each}}
  </ul>
{{/pagination}}
{{outlet}}
